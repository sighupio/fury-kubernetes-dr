apiVersion: apps/v1
kind: Deployment
metadata:
  name: ark
spec:
  template:
    spec:
      nodeSelector:
        beta.kubernetes.io/os: linux
      containers:
      - name: ark
        envFrom:
        - secretRef:
            name: cloud-credentials
